import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export const ADD_USERS = 'ADD_USERS';

export default new Vuex.Store({
  state: {
    color: '#e55934',
  },
  mutations: {},
  actions: {},
  getters: {}
});